apiVersion: v1
kind: Service
metadata:
  name: {{ include "html-chart.fullname" . }}
  labels:
    {{- include "html-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "html-chart.selectorLabels" . | nindent 4 }}
