apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "html-chart.fullname" . }}-test-connection"
  labels:
    {{- include "html-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "html-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
